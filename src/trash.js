function Trash(obj) {
    this.pos = obj.pos
    this.trashType = obj.trashType
}

Trash.prototype.move = function() {
    //will be needed for when the user grabs the trash and moves around the map with it
}







module.exports = Trash